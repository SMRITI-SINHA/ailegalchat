You are integrating a fully rigged 3D robot avatar into a React-based legal AI voice assistant app.
The robot model is a custom character named LexAI. All assets are pre-built and ready to use.

FILES TO UPLOAD TO YOUR REPLIT PROJECT
/public/
  LexAI_Robot_Final.glb     ← 3D robot model (rigged + 31 animations + 3 props)
  face_happy.png             ← LED face: idle/greeting
  face_talking.png           ← LED face: speaking
  face_thinking.png          ← LED face: processing/thinking
  face_surprised.png         ← LED face: alert/new message
  face_serious.png           ← LED face: focused/legal mode
  face_excited.png           ← LED face: celebration/success

DEPENDENCIES TO INSTALL
bashnpm install three @react-three/fiber @react-three/drei

ANIMATION NAME → VOICE BOT STATE MAPPING
Animation NameTrigger WhenLoop?DurationIdleDefault waiting state✅1.1sHover_FloatBackground idle (more alive than Idle)✅3.0sHead_ListenUser is speaking / mic is active❌2.0sSpeaking_LoopAI is responding / TTS is playing✅2.0sThinking_PoseAI is processing / waiting for API❌2.5sWave_HelloFirst load / user opens the chat❌2.0sAgree_GestureAI confirms / says "yes" / acknowledges❌—AlertNew notification / important message❌—Confused_ShrugAI doesn't understand / error state❌2.0sPoint_ForwardAI gives a directive / "you should..."❌2.0sExcited_BounceTask complete / good legal outcome❌1.5sSpin_EntranceFirst load after Wave_Hello❌1.5sAriseWake word detected / activation❌—Casual_WalkLoading screen / intro✅—
PROP VISIBILITY MAPPING
Prop Node NameShow WhenAnimationSpeechBubbleAI is speaking (TTS active)Fade in/out via scaleGavelLegal decision made / case resolvedGavel_SwingParticleAuraCelebration / success / special modeParticleAura_Spin
LED FACE TEXTURE MAPPING
Texture FileBot Stateface_happy.pngIdle, greeting, listeningface_talking.pngSpeaking / TTS activeface_thinking.pngAPI processing / loadingface_surprised.pngNew message / alertface_serious.pngAnalyzing document / legal reviewface_excited.pngSuccess / task complete

COMPLETE COMPONENT CODE
Create /src/components/LexAIRobot.jsx:
jsximport React, { useRef, useEffect, useState, useCallback } from 'react'
import { Canvas, useFrame } from '@react-three/fiber'
import { useGLTF, useAnimations, Environment } from '@react-three/drei'
import * as THREE from 'three'

// ─── BOT STATES ──────────────────────────────────────────────────────────────
export const BOT_STATE = {
  IDLE:       'idle',
  LISTENING:  'listening',
  THINKING:   'thinking',
  SPEAKING:   'speaking',
  SUCCESS:    'success',
  ALERT:      'alert',
  ERROR:      'error',
  GREETING:   'greeting',
  DECISION:   'decision',
}

// ─── STATE → ANIMATION MAP ───────────────────────────────────────────────────
const STATE_ANIMATION = {
  [BOT_STATE.IDLE]:      'Hover_Float',
  [BOT_STATE.LISTENING]: 'Head_Listen',
  [BOT_STATE.THINKING]:  'Thinking_Pose',
  [BOT_STATE.SPEAKING]:  'Speaking_Loop',
  [BOT_STATE.SUCCESS]:   'Excited_Bounce',
  [BOT_STATE.ALERT]:     'Alert',
  [BOT_STATE.ERROR]:     'Confused_Shrug',
  [BOT_STATE.GREETING]:  'Wave_Hello',
  [BOT_STATE.DECISION]:  'Point_Forward',
}

// ─── STATE → FACE MAP ────────────────────────────────────────────────────────
const STATE_FACE = {
  [BOT_STATE.IDLE]:      '/face_happy.png',
  [BOT_STATE.LISTENING]: '/face_happy.png',
  [BOT_STATE.THINKING]:  '/face_thinking.png',
  [BOT_STATE.SPEAKING]:  '/face_talking.png',
  [BOT_STATE.SUCCESS]:   '/face_excited.png',
  [BOT_STATE.ALERT]:     '/face_surprised.png',
  [BOT_STATE.ERROR]:     '/face_surprised.png',
  [BOT_STATE.GREETING]:  '/face_happy.png',
  [BOT_STATE.DECISION]:  '/face_serious.png',
}

// ─── ROBOT INNER COMPONENT ───────────────────────────────────────────────────
function RobotModel({ botState, audioAmplitude = 0 }) {
  const group = useRef()
  const { scene, animations } = useGLTF('/LexAI_Robot_Final.glb')
  const { actions, mixer } = useAnimations(animations, group)
  
  const currentActionRef = useRef(null)
  const faceTextureRef = useRef(null)
  const faceMeshRef = useRef(null)
  const speechBubbleRef = useRef(null)
  const gavelRef = useRef(null)
  const auraRef = useRef(null)
  const auraActionRef = useRef(null)
  const gavelActionRef = useRef(null)
  const prevStateRef = useRef(null)

  // ── Find prop nodes on mount ──────────────────────────────────────────────
  useEffect(() => {
    if (!scene) return
    scene.traverse((obj) => {
      if (obj.name === 'SpeechBubble') speechBubbleRef.current = obj
      if (obj.name === 'Gavel') gavelRef.current = obj
      if (obj.name === 'ParticleAura') auraRef.current = obj
      
      // Find the face screen mesh (has headfront as parent bone)
      if (obj.isMesh && obj.material) {
        // The main mesh with textures
        if (obj.material.map) faceMeshRef.current = obj
      }
    })

    // Hide all props initially
    if (speechBubbleRef.current) speechBubbleRef.current.visible = false
    if (gavelRef.current) gavelRef.current.visible = false
    if (auraRef.current) auraRef.current.visible = false

    // Start with greeting
    playAnimation('Wave_Hello', false)
    setTimeout(() => playAnimation('Hover_Float', true), 2200)
    
    // Setup aura animation ref
    auraActionRef.current = actions['ParticleAura_Spin']
    gavelActionRef.current = actions['Gavel_Swing']
  }, [scene, actions])

  // ── Play animation with crossfade ─────────────────────────────────────────
  const playAnimation = useCallback((name, loop = true) => {
    const action = actions[name]
    if (!action) return
    
    if (currentActionRef.current && currentActionRef.current !== action) {
      currentActionRef.current.fadeOut(0.35)
    }
    
    action.reset()
    action.setLoop(loop ? THREE.LoopRepeat : THREE.LoopOnce, loop ? Infinity : 1)
    action.clampWhenFinished = !loop
    action.fadeIn(0.35).play()
    currentActionRef.current = action
  }, [actions])

  // ── Update face texture ───────────────────────────────────────────────────
  const updateFace = useCallback((texturePath) => {
    if (!faceMeshRef.current) return
    const loader = new THREE.TextureLoader()
    loader.load(texturePath, (tex) => {
      tex.encoding = THREE.sRGBEncoding
      // Apply as emissive overlay on the face screen area
      // The face LED screen is controlled via emissiveMap
      if (faceMeshRef.current.material) {
        // Create a clone to avoid modifying shared material
        if (!faceTextureRef.current) {
          faceMeshRef.current.material = faceMeshRef.current.material.clone()
        }
        faceMeshRef.current.material.emissiveMap = tex
        faceMeshRef.current.material.emissive = new THREE.Color(0.8, 0.5, 0.1)
        faceMeshRef.current.material.emissiveIntensity = 1.2
        faceMeshRef.current.material.needsUpdate = true
        faceTextureRef.current = tex
      }
    })
  }, [])

  // ── React to bot state changes ────────────────────────────────────────────
  useEffect(() => {
    if (!actions || botState === prevStateRef.current) return
    prevStateRef.current = botState

    // Play animation
    const animName = STATE_ANIMATION[botState]
    const isLoop = [BOT_STATE.IDLE, BOT_STATE.SPEAKING, BOT_STATE.LISTENING].includes(botState)
    if (animName) playAnimation(animName, isLoop)

    // Update face texture
    const faceTex = STATE_FACE[botState]
    if (faceTex) updateFace(faceTex)

    // Handle props
    // Speech Bubble
    if (speechBubbleRef.current) {
      speechBubbleRef.current.visible = botState === BOT_STATE.SPEAKING
    }

    // Gavel + swing
    if (gavelRef.current) {
      const showGavel = botState === BOT_STATE.DECISION
      gavelRef.current.visible = showGavel
      if (showGavel && gavelActionRef.current) {
        gavelActionRef.current.reset().play()
      }
    }

    // Particle Aura
    if (auraRef.current) {
      const showAura = botState === BOT_STATE.SUCCESS
      auraRef.current.visible = showAura
      if (showAura && auraActionRef.current) {
        auraActionRef.current.reset()
          .setLoop(THREE.LoopRepeat, Infinity)
          .play()
      } else if (!showAura && auraActionRef.current) {
        auraActionRef.current.stop()
      }
    }
  }, [botState, actions, playAnimation, updateFace])

  // ── Procedural animation frame ────────────────────────────────────────────
  useFrame((state) => {
    if (!group.current) return
    const t = state.clock.elapsedTime

    // Head tracking toward cursor (subtle)
    const mx = (state.mouse.x * 0.15)
    const my = (state.mouse.y * 0.08)
    
    // Find head bone
    scene.traverse((obj) => {
      if (obj.name === 'Head' && obj.isBone) {
        obj.rotation.y += (mx - obj.rotation.y) * 0.05
        obj.rotation.x += (my - obj.rotation.x) * 0.05
      }
    })

    // Voice amplitude drives head bob
    if (botState === BOT_STATE.SPEAKING && audioAmplitude > 0) {
      scene.traverse((obj) => {
        if (obj.name === 'Head' && obj.isBone) {
          obj.rotation.x += Math.sin(t * 8) * audioAmplitude * 0.04
        }
      })
    }

    // Random blink via headfront bone scale
    const blinkCycle = Math.sin(t * 0.7) * Math.sin(t * 1.3)
    if (blinkCycle > 0.98) {
      scene.traverse((obj) => {
        if (obj.name === 'headfront') {
          obj.scale.y = 0.1  // squish = blink
        }
      })
    } else {
      scene.traverse((obj) => {
        if (obj.name === 'headfront') {
          obj.scale.y += (1.0 - obj.scale.y) * 0.3
        }
      })
    }

    // Speech bubble gentle float
    if (speechBubbleRef.current && speechBubbleRef.current.visible) {
      speechBubbleRef.current.position.y = 0.6 + Math.sin(t * 2) * 0.03
    }
  })

  return (
    <group ref={group}>
      <primitive object={scene} />
    </group>
  )
}

// ─── MAIN EXPORT COMPONENT ───────────────────────────────────────────────────
export default function LexAIRobot({ 
  botState = BOT_STATE.IDLE,
  audioAmplitude = 0,
  className = ''
}) {
  return (
    <div className={`lexai-robot-canvas ${className}`} style={{ width: '100%', height: '100%' }}>
      <Canvas
        camera={{ position: [0, 1.2, 3.2], fov: 42 }}
        gl={{ antialias: true, alpha: true }}
        onCreated={({ gl }) => {
          gl.outputEncoding = THREE.sRGBEncoding
          gl.toneMapping = THREE.ACESFilmicToneMapping
          gl.toneMappingExposure = 1.15
        }}
      >
        {/* Lighting */}
        <ambientLight intensity={0.7} color="#fff5f0" />
        <directionalLight position={[2, 4, 3]} intensity={1.6} color="#fffaf5" castShadow />
        <directionalLight position={[-2, 2, -1]} intensity={0.5} color="#e0eeff" />
        <pointLight position={[0, 3, -2]} intensity={0.4} color="#ffc8a0" />
        
        {/* Robot */}
        <RobotModel botState={botState} audioAmplitude={audioAmplitude} />
        
        {/* Environment for reflections */}
        <Environment preset="city" />
      </Canvas>
    </div>
  )
}

// Preload model
useGLTF.preload('/LexAI_Robot_Final.glb')

HOW TO WIRE IT TO YOUR VOICE BOT
jsx// In your main chat/voice component:
import LexAIRobot, { BOT_STATE } from './components/LexAIRobot'
import { useState, useRef, useEffect } from 'react'

function VoiceBotPage() {
  const [botState, setBotState] = useState(BOT_STATE.IDLE)
  const [amplitude, setAmplitude] = useState(0)
  const analyserRef = useRef(null)
  const animFrameRef = useRef(null)

  // ── Set up Web Audio amplitude analyzer ───────────────────────────────────
  const startAudioAnalysis = (audioElement) => {
    const ctx = new AudioContext()
    const source = ctx.createMediaElementSource(audioElement)
    const analyser = ctx.createAnalyser()
    analyser.fftSize = 256
    source.connect(analyser)
    analyser.connect(ctx.destination)
    analyserRef.current = analyser

    const data = new Uint8Array(analyser.frequencyBinCount)
    const tick = () => {
      analyser.getByteFrequencyData(data)
      const avg = data.reduce((a, b) => a + b, 0) / data.length
      setAmplitude(avg / 255)  // normalize 0-1
      animFrameRef.current = requestAnimationFrame(tick)
    }
    tick()
  }

  // ── Connect to your AI/TTS events ─────────────────────────────────────────
  // Call these functions based on your voice bot's events:
  
  const onUserStartSpeaking = () => setBotState(BOT_STATE.LISTENING)
  const onUserStopSpeaking  = () => setBotState(BOT_STATE.THINKING)
  const onAIStartSpeaking   = (audioEl) => {
    setBotState(BOT_STATE.SPEAKING)
    startAudioAnalysis(audioEl)
  }
  const onAIStopSpeaking    = () => {
    setBotState(BOT_STATE.IDLE)
    setAmplitude(0)
    if (animFrameRef.current) cancelAnimationFrame(animFrameRef.current)
  }
  const onTaskComplete      = () => {
    setBotState(BOT_STATE.SUCCESS)
    setTimeout(() => setBotState(BOT_STATE.IDLE), 2500)
  }
  const onLegalDecision     = () => {
    setBotState(BOT_STATE.DECISION)
    setTimeout(() => setBotState(BOT_STATE.IDLE), 3000)
  }
  const onError             = () => {
    setBotState(BOT_STATE.ERROR)
    setTimeout(() => setBotState(BOT_STATE.IDLE), 2000)
  }

  return (
    <div style={{ display: 'grid', gridTemplateColumns: '420px 1fr', height: '100vh' }}>
      {/* Robot Panel */}
      <div style={{ background: '#0d0d18', position: 'relative' }}>
        <LexAIRobot 
          botState={botState}
          audioAmplitude={amplitude}
          className="h-full"
        />
        {/* State label (dev only - remove in prod) */}
        <div style={{ position:'absolute', bottom:16, left:0, right:0, textAlign:'center', color:'#555', fontSize:12 }}>
          {botState.toUpperCase()}
        </div>
      </div>

      {/* Your chat UI here */}
      <div style={{ padding: 32 }}>
        {/* ... your existing chat interface ... */}
      </div>
    </div>
  )
}

PERFORMANCE NOTES FOR REPLIT
jsx// Add this to your vite.config.js or webpack config:
// GLB files should be served from /public folder
// They will be available at /LexAI_Robot_Final.glb

// Lazy load the 3D canvas to not block initial page render:
import { lazy, Suspense } from 'react'
const LexAIRobot = lazy(() => import('./components/LexAIRobot'))

// Wrap with Suspense:
<Suspense fallback={<div className="robot-placeholder">Loading Lex...</div>}>
  <LexAIRobot botState={botState} audioAmplitude={amplitude} />
</Suspense>

ANIMATION STATE MACHINE (Simple Version)
                    ┌─────────────────────────────┐
                    │         IDLE / HOVER         │ ← default loop
                    └──────────────┬──────────────┘
                                   │
              ┌────────────────────┼────────────────────┐
              │                    │                     │
         user speaks          new message           first load
              │                    │                     │
              ▼                    ▼                     ▼
         LISTENING              ALERT               GREETING
              │                    │                (Wave_Hello)
         user stops           acknowledged               │
              │                    │               SPIN_ENTRANCE
              ▼                    ▼                     │
          THINKING  ◄─────────────┘                 → IDLE
              │
         API responds
              │
              ▼
          SPEAKING ──── TTS active ────► [audio amplitude → head bob]
              │           │
              │      show SpeechBubble
              │
         TTS ends
              │
        ┌─────┴──────┐
        │            │
     normal      legal decision
        │            │
      IDLE        DECISION
                  (Gavel_Swing)
                      │
                   SUCCESS
                  (Particle Aura)
                      │
                    IDLE

TROUBLESHOOTING
Model appears white/untextured:
→ Ensure gl.outputEncoding = THREE.sRGBEncoding is set in Canvas onCreated
→ Ensure tex.encoding = THREE.sRGBEncoding when loading face textures
Animations not blending smoothly:
→ Increase fadeIn/fadeOut from 0.35 to 0.5 for slower transitions
Props not showing:
→ Check that GLB is loaded from /public/ folder (not /src/)
→ Confirm node names: SpeechBubble, Gavel, ParticleAura
Model too small or large:
→ Adjust Camera fov (currently 42) or add <primitive object={scene} scale={1.5} />
Performance issues on Replit:
→ Add frameloop="demand" to Canvas to only render on state changes
→ Use <Suspense> wrapper with fallback UI
→ Compress GLB further: npx gltf-pipeline -i LexAI_Robot_Final.glb -o LexAI_compressed.glb --draco.compressionLevel 7